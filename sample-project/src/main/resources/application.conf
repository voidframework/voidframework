voidframework {
    core {
        acceptedScanPaths += "controller"
        acceptedScanPaths += "converter"
        acceptedScanPaths += "service"

        #disabledModules += "com.voidframework.web.healthcheck.module.HealthCheckControllerModule"
    }

    cache {
        engine = "com.voidframework.cache.engine.MemoryCacheEngine"
    }
}
